"use strict";

const generateToken = async () => {
    try {
        //do stuff
    } catch (err) {
        console.error(err);
    }
};

const verifyToken = async () => {
    try {
        //do something
    } catch (err) {
        console.log(err);
    }
};

export default {
    generateToken, 
    verifyToken
};